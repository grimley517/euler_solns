import math

def Digit(number, divosor):
    answer = number/math.factorial(divisor))
    return answer
def leftover(number, divisor):
    num = number - math.floor(number)
    answer = num * math.factorial(divisor)
    return answer
def result(number):
    answer = ''
    for i in range (10):
        answer.append(digit(number, i))
        number=leftover(
        
